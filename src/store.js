import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // 상태 정의
  },
  mutations: {
    // 상태 변경을 위한 메소드 정의
  },
  actions: {
    // 비동기 로직 실행을 위한 메소드 정의
  }
});